#!/usr/bin/env python3

def sum_to_k(lst, k): 
   left = 0
   right = len(lst) - 1

   while left < right:
      if a[left] + a[right]:
         right = right - 1
      elif a[left] + 

lst = [1, 50, 65, 7, 50]
k = 100
print(sum_to_k(lst, k))
